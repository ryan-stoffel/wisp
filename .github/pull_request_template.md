## Summary

<!-- What changed and why. -->

Closes #

## Acceptance criteria

<!-- Restate the issue's acceptance criteria as a checklist. -->

- [ ]

## How it was tested

<!-- Commands run, and what they showed. -->

## Visuals

<!--
Only for a change a user can see. For backend, CI, docs, refactor, and test-only PRs, leave the block below as it is
and do not add the screenshots label.

For visuals, add the screenshots label and uncomment one line per mode in the block below, naming scenes from
ci/screenshots/src/scenarios.ts, separated by commas, each scene once, at most 10. Add a scene there in this PR if the
view is new.
- after: a new view or flow, captured from this PR's app.
- before-after: a user-visible bug fix, captured from the base commit's app and from this PR's app, side by side.
- video: behavior that only makes sense in motion, recorded as a video with a GIF preview.

The screenshots workflow writes the result into a Screenshots section at the end of this description and replaces it
on every push while the label is on, so keep your own text outside it. With the label, a block that names no scene or
an unknown one fails the workflow with a message saying what to fix. Removing the label stops new captures and leaves
the section in place.
-->

<!-- wisp-media
# after: agents-window-project
# before-after: agents-window-disconnected
# video: agents-window-subagent
-->
